---
- name: check postfix status
  hosts: web
  tasks:

  - name: check_postfix
    ansible.builtin.shell:
      cmd: systemctl status postfix 
    rgister: result

  - name: Display result
    debug:
      msg: "{{result.stdout_lines}}"
  
